#!/usr/bin/env bash
set -euo pipefail

delay="${HYPRWAYSPACES_WAYBAR_RELOAD_DELAY:-0.25}"

sleep "$delay"
hyprctl reload >/dev/null 2>&1 || true

# Waybar custom modules expect output; keep this module invisible.
printf ''
