#!/usr/bin/env bash
REPO_ROOT="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/.." && pwd)"
ctx=$(cat "$REPO_ROOT/generated/current-context" 2>/dev/null || echo "a")
hyprctl dispatch togglespecialworkspace "scratch-${ctx}"
